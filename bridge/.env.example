# Port the bridge listens on (default: 3131)
PORT=3131

# Default project path the bridge runs Claude against when the request omits one
PROJECT_PATH=/path/to/your/project

# Roots under which a page may name its own project with
# <meta name="claude-inspector-project" content="my-app">.
# The name is resolved under these roots and nothing outside them is accepted;
# with none declared, pages cannot choose the project at all.
# Overrides ~/.claude-inspector/config.json ({"roots": ["~/Projects"]}).
# INSPECTOR_PROJECT_ROOTS=/Users/you/Projects

# Optional: explicit path to the Claude Code binary.
# Leave unset to let the Agent SDK auto-detect its bundled binary in node_modules.
# Use this only if the bundled binary cannot be found.
# CLAUDE_PATH=/Users/you/.local/bin/claude
